{% extends "base.html.twig" %}

{% block body %}

{% for label, msgs in app.flashes %}
		{% for msg in msgs %}
			<div class="alert alert-{{ label }} text-center">
				{{ msg }}
			</div>

		{% endfor %}
	{% endfor %}

<h1 class="text-center">Liste des commandes</h1><br><br>

<table class="table">
<thead>
    <tr>
        
        <th>Chambre</th>
        <th>date d'arrivée</th>
        <th>date de départ</th>
        <th>prix total</th>
        <th>prenom</th>
        <th>nom</th>
        <th>telephone</th>
        <th>email</th>
        <th>date d'enregistrement</th>
        {# <th>action</th> #}



    </tr>
</thead>
<tbody>
{% for item in commandes %}
    <tr>
        
        <td>{{ item.dateEnregistrement|date('d/m/Y à H:i:s') }}</</td>
        <td>{{ item.chambre.titre }}</td>

        
        <td>{{ item.chambre.titre }}</td>
        <td>{{ item.dateArrivee|date('d/m/Y à H:i:s') }}</</td>
        <td>{{ item.dateDepart|date('d/m/Y à H:i:s') }}</td>
        {# <td>{{ item.prixTotal }}€</td> #}
        <td>{{ item.user.prenom }}</td>
        <td>{{ item.user.nom }}</td>
        {# <td>{{ item.user.telephone }}</</td> #}
        <td>{{ item.user.email }}</td>
        <td>{{ item.dateEnregistrement|date('d/m/Y à H:i:s') }}</</td>   
        



    </tr>
{% endfor %}
</tbody>
</table>

{% endblock %}